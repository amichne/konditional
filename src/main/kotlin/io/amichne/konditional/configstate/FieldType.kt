package io.amichne.konditional.configstate

enum class FieldType {
    FLAG_VALUE,
    FLAG_ACTIVE,
    SALT,
    RAMP_UP_PERCENT,
    RAMP_UP_ALLOWLIST,
    LOCALES,
    PLATFORMS,
    SEMVER,
    VERSION_RANGE,
    AXES_MAP,
    RULE_NOTE,
}
