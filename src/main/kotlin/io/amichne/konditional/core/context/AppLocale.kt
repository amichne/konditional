package io.amichne.konditional.core.context

// ---------- Locale and Market enums (examples only) ----------
enum class AppLocale(
    val isoLanguageCountryCodes: String
) {
    EN_US("en-US"),
    ES_US("es-US"),
    EN_CA("en-CA"),
    HI_IN("hi-IN"),
}
