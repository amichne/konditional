# Legacy (Unrendered) Documentation

This directory contains the previous Docusaurus-rendered docs that used to live under `docusaurus/docs/`.

They are kept for reference/history, but the rendered user-facing documentation now lives in `docs/` at the repo root.

