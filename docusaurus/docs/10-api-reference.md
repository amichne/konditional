# API Reference

The API reference is rendered by Scalar at `/api` using `static/openapi.json` (served at `/konditional/openapi.json`).

To refresh it, regenerate `static/openapi.json` from your source schema and reload the site.
