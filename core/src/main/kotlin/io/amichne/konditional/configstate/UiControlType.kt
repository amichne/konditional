package io.amichne.konditional.configstate

enum class UiControlType {
    TOGGLE,
    TEXT,
    TEXTAREA,
    NUMBER,
    PERCENT,
    SELECT,
    MULTISELECT,
    KEY_VALUE,
    JSON,
    SEMVER,
    SEMVER_RANGE,
}
